config KSU
	bool "KernelSU module"
	default y
	depends on KPROBES
	select OVERLAY_FS
	help
	This is the KSU privilege driver for android system.

config KSU_DEBUG
	bool "KernelSU module debug mode"
	default n
	depends on KSU
	help
	This enables debug mode for KSU
